interface:
  display_name: "道 Skill"
  short_description: "归根、生成并进化 skill"
  default_prompt: "Use $dao-skill to turn this idea or feedback into a rooted, runnable, evolvable Codex skill."
policy:
  allow_implicit_invocation: true
