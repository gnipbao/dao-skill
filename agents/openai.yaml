interface:
  display_name: "道 · Skill"
  short_description: "归根、生成、评估并进化 Agent Skill"
  default_prompt: "Use $dao-skill to turn this idea, artifact, evidence, or feedback into a rooted, runnable, verifiable Skill. Do not write outside the current project, install globally, commit, or publish unless the user explicitly requests that action."
policy:
  allow_implicit_invocation: true
